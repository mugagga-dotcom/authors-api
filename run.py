from app import create_app
app = create_app()
if __name__ == "__main__":app.run(debug=True) #updating the debug mode to true
#Its the entry point to run your code.
#It separates running logic from the setup.
